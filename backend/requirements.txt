fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
pytest==8.3.3
httpx==0.27.2

# Opcional — só necessário se REDIS_URL estiver configurada (rate limiter
# distribuído pra múltiplas instâncias, ver app/core/rate_limiter_redis.py).
# Sem Redis configurado, o app funciona normalmente com o rate limiter em
# memória (uma instância só).
redis==5.0.1
